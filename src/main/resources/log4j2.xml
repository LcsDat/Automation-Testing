<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <!-- ANSI color codes -->
        <Property name="CONSOLE_LOG_PATTERN">⏱️ %style{%d{HH:mm:ss a}}{bright,cyan} [%t] %highlight{%level{FATAL=☠️, ERROR=❌, WARN=⚠️, INFO=ℹ️, DEBUG=🔍, TRACE=📋}} %logger{36} - %style{%msg%n}{yellow} </Property>
    </Properties>
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${CONSOLE_LOG_PATTERN}" disableAnsi="false"/>
        </Console>
    </Appenders>
    <Loggers>
        <Root level="debug">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>