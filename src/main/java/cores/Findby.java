package cores;

import org.openqa.selenium.By;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Findby {

//    public static By autoDetect(String locator) {
//        By by;
//        if (locator.startsWith("/")) {
//            by = By.xpath(locator);
//        } else {
//            if (locator.startsWith("#") || locator.startsWith(".")) {
//                by = By.cssSelector(locator);
//            } else {
//                try {
//                    boolean flag = true;
//                    List a = Arrays.asList(By.cssSelector(locator), By.className(locator),By.id(locator),By.name(locator));
//                    for (int i = 0; i < a.size() -1; i++) {
//                        by = (By) a.get(i);
//                    }
//                } catch (Exception e){
//
//                }
//            }
//        }
//
//        return by;
//    }
}
